<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.MyBudgetApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/disabled_text</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/colorSecondary</item>
        <item name="colorSecondaryVariant">#bababa</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Customize your light theme here. -->
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:spotShadowAlpha">0.08</item>
        <item name="android:ambientShadowAlpha">0.08</item>
        <item name="android:colorControlHighlight">@color/colorControlHighlight</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/bg_01dp</item>
        <item name="android:windowLightStatusBar">true</item>
        <!-- Bottom bar indicator -->
        <item name="colorSecondaryContainer">@color/colorSecondaryContainer</item>
        <!-- Custom material 3 -->
        <item name="materialSwitchStyle">@style/Widget.App.Switch</item>
        <item name="radioButtonStyle">@style/Widget.App.RadioButton</item>
        <item name="materialCalendarTheme">@style/ThemeOverlay.App.DatePicker</item>
        <item name="materialAlertDialogTheme">@style/ThemeOverlay.AlertDialog</item>
        <!--header text color-->
        <item name="android:textColorPrimaryInverse">#FFFFFF</item>
        <!--days of the week-->
        <item name="android:textColorSecondary">@color/medium_emphasis_text</item>
    </style>

    <!-- ALERT DIALOG -->
    <style name="ThemeOverlay.AlertDialog" parent="ThemeOverlay.Material3.MaterialAlertDialog">
        <item name="colorPrimary">@color/high_emphasis_text</item>
    </style>

    <style name="AlertDialogTheme" parent="AppTheme">
        <item name="colorPrimary">@color/high_emphasis_text</item>
    </style>

    <!-- SWITCH BUTTON -->

    <style name="Widget.App.Switch" parent="Widget.Material3.CompoundButton.Switch">
        <item name="materialThemeOverlay">@style/ThemeOverlay.App.Switch</item>
    </style>

    <style name="ThemeOverlay.App.Switch" parent="">
        <!-- Thumb -->
        <item name="colorPrimary">@color/switch_thumb_on</item>
        <item name="colorOnSurface">@color/switch_thumb_off</item>
        <!-- Track -->
        <item name="colorPrimaryContainer">@color/switch_track_on</item>
        <item name="colorOutline">@color/switch_track_off</item>
    </style>

    <!-- RADIO BUTTON -->

    <style name="Widget.App.RadioButton" parent="Widget.Material3.CompoundButton.RadioButton">
        <item name="materialThemeOverlay">@style/ThemeOverlay.App.RadioButton</item>
    </style>

    <style name="ThemeOverlay.App.RadioButton" parent="">
        <item name="colorPrimary">@color/button_accent</item>
    </style>

    <!-- Date Picker -->
    <style name="ThemeOverlay.App.DatePicker" parent="@style/ThemeOverlay.Material3.MaterialCalendar">
        <item name="colorPrimary">@color/savings</item>
        <item name="colorOnPrimary">@color/white</item>
    </style>

    <!-- Splash screen -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- Set the splash screen background, animated icon, and animation duration. -->
        <item name="windowSplashScreenBackground">@color/bg_splash_screen</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/app_icon</item>
        <!-- Set the theme of the Activity that directly follows your splash screen. -->
        <item name="postSplashScreenTheme">@style/AlertDialogTheme</item>
    </style>

    <style name="AppTheme" parent="Base.Theme.MyBudgetApp" />
</resources>